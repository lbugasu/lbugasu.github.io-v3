export const getFeatureImage = (state) => state.featuredImage;
export const getPosts = (state) => state.posts;
export const getTags = (state) => state.tags;
export const getMode = (state) => state.mode;
